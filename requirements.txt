# Dépendances de web-crew — versions épinglées pour un environnement reproductible.
# Installation : pip install -r requirements.txt

anthropic==0.111.0
typer==0.23.2
python-dotenv==1.2.1
pypdf==6.1.1
# Requis par pypdf pour sortir les images d'un PDF. Sans lui, pypdf lève
# « pillow is required to do image extraction » ; extraire_images() avale
# l'exception et le document ne rend aucune photo, sans erreur visible.
pillow==12.3.0
python-docx==1.2.0

# Critique visuelle (optionnel) — pilote un navigateur pour photographier le
# site généré. Après l'installation du paquet, télécharger le navigateur une
# fois :  playwright install chromium
# Sans lui, toutes les autres commandes fonctionnent normalement.
playwright==1.62.0

# Orchestration V2 — la machine à états qui remplace le pipeline linéaire.
# LangGraph sert d'ORDONNANCEUR seulement : les nœuds appellent le SDK anthropic
# directement, jamais les abstractions de modèle de LangChain. C'est ce qui
# permet de garder `effort`, le raisonnement adaptatif et le comptage exact.
# langchain-core arrive comme dépendance de langgraph : présent, mais pas utilisé.
langgraph==1.2.11
# Le checkpointer SQLite : l'état du graphe survit à l'arrêt du processus, donc
# un run interrompu reprend au nœud fautif au lieu de tout repayer.
langgraph-checkpoint-sqlite==3.1.1

# Développement
pytest==8.4.2
